async 
- should use with javascript which doesnt depend or which is not related to any HTML, doesnt work on HTML element on your HTML page.
  • if it is an external script which doesnt have anything to do with HTML element or the DOM.
defer 
- if its not external script.